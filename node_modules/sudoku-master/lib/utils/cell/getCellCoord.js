"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const ramda_1 = require("ramda");
const house_1 = require("../house");
const getCellIndexInGrid_1 = require("./getCellIndexInGrid");
const fp_1 = require("../fp");
/**
 * Determinate the coordinates (row and column) of a given cell.
 *
 * @private
 * @since 0.0.2
 *
 * @param {CellCoord | GridIndex | CellHouse} cell The cell for which to find the coordinates.
 * @returns {CellCoord} A tuple containing the index of the row and column of the given cell.
 *
 * @example
 * getCellCoord([4, 3]);
 * // => [4, 3]
 * getCellCoord(49);
 * // => [5, 4]
 * getCellCoord({ houseType: "row", houseIndex: 8, cellIndex: 8 });
 * // => [8, 8]
 */
function getCellCoord(cell) {
    return ramda_1.cond([
        [Array.isArray, (cellCoord) => cellCoord],
        [ramda_1.is(Number), (gridIndex) => [house_1.getCellRow(gridIndex).index, house_1.getCellCol(gridIndex).index]],
        [fp_1.isTypeOf("object"), (cellHouse) => getCellCoord(getCellIndexInGrid_1.getCellIndexInGrid(cellHouse))],
    ])(cell);
}
exports.getCellCoord = getCellCoord;
