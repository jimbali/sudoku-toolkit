export declare type SubsetLevel = 2 | 3 | 4;
export declare type SubsetData<X, Y> = {
    readonly subsetIndexes: readonly X[];
    readonly subsetValues: readonly Y[];
};
export declare type LookupData<T extends object, X, Y> = T & {
    readonly lookup: ReadonlyMap<X, readonly Y[]>;
};
export declare type FindSubsetResult<T extends object, X, Y> = LookupData<T, X, Y> & {
    readonly result: SubsetData<X, Y>;
};
