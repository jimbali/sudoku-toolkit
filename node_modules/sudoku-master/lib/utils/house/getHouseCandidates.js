"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const candidate_1 = require("../candidate");
const remeda_1 = require("remeda");
const ramda_1 = require("ramda");
const _getHouseCandidates = (house, candidates) => {
    const houseCells = ramda_1.values(house.cells);
    return candidate_1.getCandidatesIndexes(new Map(remeda_1.filter([...candidates.entries()], (entry) => ramda_1.includes(entry[0], houseCells))));
};
// eslint-disable-next-line functional/functional-parameters,@typescript-eslint/explicit-function-return-type
function getHouseCandidates() {
    // eslint-disable-next-line prefer-rest-params,functional/functional-parameters
    return remeda_1.purry(_getHouseCandidates, arguments);
}
exports.getHouseCandidates = getHouseCandidates;
