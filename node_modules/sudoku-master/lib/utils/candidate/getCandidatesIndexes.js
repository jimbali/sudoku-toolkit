"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const types_1 = require("../../types");
const remeda_1 = require("remeda");
const getCellsContainingCandidate_1 = require("./getCellsContainingCandidate");
/**
 * Associate each digit in the grid with the indexes on which they are a candidate in the grid.
 *
 * @private
 * @since 0.0.2
 *
 * @param candidates The list of candidates in the grid.
 * @returns A key/value map that associate each digit with the indexes in the grid in which they are a candidate.
 */
function getCandidatesIndexes(candidates) {
    const findCellsContainingCandidates = getCellsContainingCandidate_1.getCellsContainingCandidate(candidates, undefined);
    return new Map(remeda_1.pipe(types_1.VALID_DIGITS, remeda_1.map((digit) => [digit, findCellsContainingCandidates(digit)]), remeda_1.filter((entry) => entry[1].length > 0)));
}
exports.getCandidatesIndexes = getCandidatesIndexes;
